odoo.define('ppts_campaign_dashboard.CampaignModel', function (require) {
    "use strict";

    var AbstractModel = require('web.BasicModel');

    var CampaignModel = AbstractModel.extend({

    });
    return CampaignModel;
});

